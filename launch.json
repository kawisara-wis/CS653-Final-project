{
  "version": "0.2.0",
  "configurations": [
    { "name": "WMS app", "type": "python", "request": "launch",
      "program": "${workspaceFolder}/app.py",
      "env": { "PYTHONPATH": "${workspaceFolder}" } }
  ]
}
